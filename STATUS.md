# make_friends 纠错审计状态

更新时间：2026-07-27

## 已完成

- 恢复 Claude 会话现场，确认原始基线为 `origin/main` 的 `ef45318`。
- 确认本地 `main` 为 `3592e99`，包含 13 个尚未推送的本地提交。
- 保留 `fix/ratelimit-and-ws-auth` 上的 3 个已修改文件和 2 个未跟踪测试文件，没有 stash、reset 或覆盖。
- 完成鉴权限流、结算、前端、推荐、热榜和运维文档六个领域的第一轮静态审计。
- 完成 `fix/ratelimit-and-ws-auth`：可信代理白名单、三类独立预算、账号失败清零、WebSocket 撤销/禁用/成员校验和隔离 Redis 测试保护均已实现。
- 使用 `127.0.0.1:16379` 的一次性 Redis 完成 WebSocket/Redis 集成测试；未访问或清空日常 `6379`。
- 当前分支通过 `git diff --check`、`go vet ./...` 和 `go test ./... -count=1`。

## 正在做

- 提交 `fix/ratelimit-and-ws-auth` 并以 `--no-ff` 合入本地 `main`。

## 下一步

1. 分支修复热榜候选截断、结算取消优先级和管理员恢复路径。
2. 分支修复聊天轮询、聊天室双初始化和管理后台末页回退。
3. 分支修复推荐 Worker 失败批次重试和 `XAUTOCLAIM` 分页。
4. 执行隔离 Redis、Go、Python、管理后台和小程序的最终验收。

## 已确认问题

| 编号 | 领域 | 问题 | 当前状态 |
| --- | --- | --- | --- |
| A-01 | 限流 | Gin 默认信任代理头，伪造 `X-Forwarded-For` 可绕过按 IP 限流 | 已修复，待合入 |
| A-02 | 限流 | 账号限流发生在密码校验前，可被用于锁死已知账号 | 已修复，待合入 |
| A-03 | 限流 | 凭证请求和自动刷新共用紧预算，不适合共享出口网络 | 已修复，待合入 |
| A-04 | WebSocket | `WSChat` 未执行 Token 撤销和禁用用户检查 | 已修复，待合入 |
| H-01 | 热榜 | 最近候选集截断会永久隐藏较老活动并截断深分页 | 待修复 |
| S-01 | 结算 | 管理员裁决优先于项目/成员取消，取消后仍可能保留完成或爽约结果 | 待修复 |
| B-01 | 管理员恢复 | 默认昵称冲突时服务静默运行且 `cmd/seed-admin` 无法恢复管理员 | 待修复 |
| F-01 | 小程序聊天 | 降级轮询每 5 秒重复拉取完整历史，长会话开销持续增长 | 待修复 |
| F-02 | 小程序聊天 | 首次进入会同时经过 `onLoad` 和 `onShow`，重复加载历史 | 待修复 |
| F-03 | 管理后台 | 删除末页最后一项后仍停留在已经不存在的空页 | 待修复 |
| R-01 | 推荐 Worker | 批次处理失败后消息留在当前 consumer 的 pending list，本进程不会再次读取 | 待修复 |
| R-02 | 推荐 Worker | 启动时 `XAUTOCLAIM` 只取一页，超过 50 条的陈旧消息仍被遗留 | 待修复 |

## 已排除问题

| 编号 | 领域 | 假设 | 结论 |
| --- | --- | --- | --- |
| X-01 | 热榜 SQL | keyword 联表后 `ORDER BY created_at` 一定产生 SQLite 歧义 | 已有实测未复现；修复热榜时仍改回原查询，不作为独立缺陷 |
| X-02 | 信誉流水 | NUL 分隔键一定导致 SQLite 比较失效 | 现有实测能够保留 `created_at` 并删除陈旧行，不改实现 |
| X-03 | 推荐训练 | 30 天窗口会遗漏窗口内曝光对应的 24 小时内点击 | 点击时间晚于曝光；窗口内曝光的合法点击也在窗口内，待增加边界测试锁定 |

## 测试矩阵

| 范围 | 命令或场景 | 状态 |
| --- | --- | --- |
| 工作区 | `git diff --check` | 通过 |
| Go 限流 | 代理伪造、账号锁定、成功清零、会话独立预算 | 通过 |
| Go WebSocket | 正常、匿名、撤销 Token、禁用用户、成员校验 | 通过（隔离 Redis `127.0.0.1:16379`） |
| Go 全量 | `go vet ./...`、`go test ./... -count=1` | 当前分支通过 |
| Python | `conda run -n agent python -m pytest backend/recommender/tests -q` | 待运行 |
| 管理后台 | 构建、搜索、401、列表和末页回退 | 待运行 |
| 小程序 | Node 测试、变更文件语法检查 | 待运行 |

## 待办但不在本轮修改

- `router.go` 和管理模块的结构性重构。
- Redis 启动失败后的后台重连。
- 小程序九处登录弹窗重复逻辑和历史 mock 死代码。
- 管理后台未接入路由的 Reviews/Ledgers 页面。
- 头像和 DiceBear 相关代码。
- 热榜的可扩展召回/排序性能设计；本轮先恢复完整语义。
