# make_friends 纠错审计状态

更新时间：2026-07-27

## 已完成

- 恢复 Claude 会话现场，确认原始基线为 `origin/main` 的 `ef45318`。
- 确认本地 `main` 为 `3592e99`，包含 13 个尚未推送的本地提交。
- 保留 `fix/ratelimit-and-ws-auth` 上的 3 个已修改文件和 2 个未跟踪测试文件，没有 stash、reset 或覆盖。
- 完成鉴权限流、结算、前端、推荐、热榜和运维文档六个领域的第一轮静态审计。
- 完成 `fix/ratelimit-and-ws-auth`：可信代理白名单、三类独立预算、账号失败清零、WebSocket 撤销/禁用/成员校验和隔离 Redis 测试保护均已实现。
- 使用 `127.0.0.1:16379` 的一次性 Redis 完成 WebSocket/Redis 集成测试；未访问或清空日常 `6379`。
- 当前分支通过 `git diff --check`、`go vet ./...` 和 `go test ./... -count=1`。
- `fix/ratelimit-and-ws-auth` 已提交为 `43f929a`，并以 `--no-ff` 合入本地 `main`。
- 完成 `fix/hot-feed-semantics`：移除 500 条候选截断和 `HOT_FEED_CANDIDATES`，恢复全部活跃活动排序/分页，并恢复仍未接入的历史缓存辅助代码。
- 热榜回归测试以 520 条活动覆盖原边界，确认最老活动仍可访问；该分支通过完整 Go 检查。
- `fix/hot-feed-semantics` 已提交为 `d7f8a6c`，并以 `--no-ff` 合入本地 `main`。
- 完成 `fix/settlement-admin-precedence`：项目取消和成员取消均优先于历史管理员裁决；重复回算后只保留对应取消流水，不再保留完成或爽约流水。
- 结算矩阵、管理员裁决持久性、取消后积分清理、重复回算、流水时间戳和案例状态测试通过；该分支通过完整 Go 检查。
- `fix/settlement-admin-precedence` 已提交为 `e614a0e`，并以 `--no-ff` 合入本地 `main`。
- 完成 `fix/admin-bootstrap-recovery`：新增 `ADMIN_INIT_NICKNAME`；无可用管理员且昵称冲突时启动/种子命令明确失败，配置未占用昵称后可安全创建新根管理员。
- 验证冲突账号不会被提权、软删除管理员不会被复活、现有密码不会被重置、重复运行保持幂等；该分支通过完整 Go 检查。
- `fix/admin-bootstrap-recovery` 已提交为 `81492b4`，并以 `--no-ff` 合入本地 `main`。
- 完成 `fix/frontend-regressions`：聊天历史支持包含边界的 `sinceCreatedAt` 和增量 `limit`，轮询按消息 ID 合并；401 会停止轮询/重连；首次 `onLoad/onShow` 共用一次初始化。
- 管理后台共享分页 Hook 会在删除末页最后一项后回退到新的最后一页；四个列表页、搜索防抖和 401 登录跳转均有 Node 回归测试。
- 聊天 API 定向测试、完整 Go 检查、小程序 4 项 Node 测试、变更 JS 语法检查、管理后台 6 项 Node 测试和生产构建均通过。
- `fix/frontend-regressions` 已提交为 `0e220e3`，并以 `--no-ff` 合入本地 `main`。
- 完成 `fix/recommender-worker-retry` 的代码实现：失败批次保留在内存并原地重试；业务已成功但 ACK 失败时只重试 ACK；曝光/点击计数仅在业务处理全部成功后提交。
- `XAUTOCLAIM` 改为沿 Redis 返回游标读取到 `0-0`，不再只处理前 50 条；新增失败重试、ACK 重试、73 条陈旧消息分页和 30 天窗口边界测试。
- 推荐 Worker、模型加载、重建和 Python 测试均未运行：本地 embedding 模型未下载，并按用户明确要求停止一切推荐代码运行验证；本分支只完成静态审阅和 `git diff --check`。

## 正在做

- 在独立分支提交 `fix/recommender-worker-retry`；提交成功后才合入本地 `main`。运行验证保持跳过。

## 下一步

1. 完成推荐 Worker 重试修复并合入本地 `main`。
2. 执行隔离 Redis、Go、Python、管理后台和小程序的最终验收。

## 已确认问题

| 编号 | 领域 | 问题 | 当前状态 |
| --- | --- | --- | --- |
| A-01 | 限流 | Gin 默认信任代理头，伪造 `X-Forwarded-For` 可绕过按 IP 限流 | 已修复并合入 |
| A-02 | 限流 | 账号限流发生在密码校验前，可被用于锁死已知账号 | 已修复并合入 |
| A-03 | 限流 | 凭证请求和自动刷新共用紧预算，不适合共享出口网络 | 已修复并合入 |
| A-04 | WebSocket | `WSChat` 未执行 Token 撤销和禁用用户检查 | 已修复并合入 |
| H-01 | 热榜 | 最近候选集截断会永久隐藏较老活动并截断深分页 | 已修复并合入 |
| S-01 | 结算 | 管理员裁决优先于项目/成员取消，取消后仍可能保留完成或爽约结果 | 已修复并合入 |
| B-01 | 管理员恢复 | 默认昵称冲突时服务静默运行且 `cmd/seed-admin` 无法恢复管理员 | 已修复并合入 |
| F-01 | 小程序聊天 | 降级轮询每 5 秒重复拉取完整历史，长会话开销持续增长 | 已修复并合入 |
| F-02 | 小程序聊天 | 首次进入会同时经过 `onLoad` 和 `onShow`，重复加载历史 | 已修复并合入 |
| F-03 | 管理后台 | 删除末页最后一项后仍停留在已经不存在的空页 | 已修复并合入 |
| R-01 | 推荐 Worker | 批次处理失败后消息留在当前 consumer 的 pending list，本进程不会再次读取 | 已修复，待合入 |
| R-02 | 推荐 Worker | 启动时 `XAUTOCLAIM` 只取一页，超过 50 条的陈旧消息仍被遗留 | 已修复，待合入 |

## 已排除问题

| 编号 | 领域 | 假设 | 结论 |
| --- | --- | --- | --- |
| X-01 | 热榜 SQL | keyword 联表后 `ORDER BY created_at` 一定产生 SQLite 歧义 | 已有实测未复现；修复热榜时仍改回原查询，不作为独立缺陷 |
| X-02 | 信誉流水 | NUL 分隔键一定导致 SQLite 比较失效 | 现有实测能够保留 `created_at` 并删除陈旧行，不改实现 |
| X-03 | 推荐训练 | 30 天窗口会遗漏窗口内曝光对应的 24 小时内点击 | 已排除；点击晚于曝光，窗口内曝光的合法点击也在窗口内；已补边界测试但按要求未运行 |

## 测试矩阵

| 范围 | 命令或场景 | 状态 |
| --- | --- | --- |
| 工作区 | `git diff --check` | 通过 |
| Go 限流 | 代理伪造、账号锁定、成功清零、会话独立预算 | 通过 |
| Go WebSocket | 正常、匿名、撤销 Token、禁用用户、成员校验 | 通过（隔离 Redis `127.0.0.1:16379`） |
| Go 全量 | `go vet ./...`、`go test ./... -count=1` | 当前分支通过 |
| Go 热榜 | 520 条活跃活动完整排序、26 页无遗漏、边界外页为空 | 通过 |
| Go 结算 | 管理员裁决、项目取消、成员取消、重复回算、流水时间戳、案例状态 | 通过 |
| Go 管理员初始化 | 默认创建、冲突明确失败、配置新昵称恢复、幂等且不修改旧账号 | 通过 |
| Python | `conda run -n agent python -m pytest backend/recommender/tests -q` | 未运行（本地模型未下载，按用户要求跳过推荐代码运行） |
| 管理后台 | 构建、搜索、401、四个列表页和末页回退 | 通过（6 项 Node 测试与生产构建） |
| 小程序 | Node 测试、变更文件语法检查 | 通过（4 项 Node 测试） |
| Go 聊天 API | 完整历史、包含边界、默认/最大增量限制、无效参数 | 通过 |

## 待办但不在本轮修改

- `router.go` 和管理模块的结构性重构。
- Redis 启动失败后的后台重连。
- 小程序九处登录弹窗重复逻辑和历史 mock 死代码。
- 管理后台未接入路由的 Reviews/Ledgers 页面。
- 头像和 DiceBear 相关代码。
- 热榜的可扩展召回/排序性能设计；本轮先恢复完整语义。
