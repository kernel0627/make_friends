<view class="sort-shell surface-card">
  <view
    wx:for="{{options}}"
    wx:key="key"
    class="sort-item {{item.key === sortBy ? 'sort-item--active' : ''}}"
    data-sort-by="{{item.key}}"
    bindtap="onSortTap"
  >
    {{item.label}}
  </view>
</view>