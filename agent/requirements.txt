# Activity Case Review Agent dependencies
# Install into conda `agent` environment

# Core graph
langgraph>=0.2.0
langchain-core>=0.3.0

# LLM (OpenAI-compatible — works with DeepSeek, OpenAI, vLLM, etc.)
openai>=1.30.0

# HTTP client (for backend API)
httpx>=0.27.0

# Config
python-dotenv>=1.0.0
pydantic>=2.0.0

# Testing
pytest>=8.0.0
